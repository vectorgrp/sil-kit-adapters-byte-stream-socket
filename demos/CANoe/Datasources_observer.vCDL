version 1.4;

import module "SilKit";

namespace Namespace
{
  [Binding="SilKit"]
  interface EchoDeviceIf
  {
    consumed data string toSocket;
  }
  
  object EchoDevice : EchoDeviceIf;
  
  [Binding="SilKit"]
  interface AdapterIf
  {
    consumed data string fromSocket;
  }
  
  object Adapter : AdapterIf;
}
